<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="library.convenie">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ConvenieActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ButtonActivity"></activity>
        <activity android:name=".EmployeeActivity"></activity>
        <activity android:name=".ExpireActivity"></activity>
        <activity android:name=".ItemsActivity"></activity>
        <activity android:name=".SaleActivity"></activity>
        <activity android:name=".TransactionActivity"></activity>
        <activity android:name=".VendorActivity"></activity>
        <activity android:name=".LoginActivity"></activity>
    </application>

</manifest>